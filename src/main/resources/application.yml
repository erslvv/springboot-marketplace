spring:
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5432/bbb
    username: market
    password: market
    driver-class-name: org.postgresql.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          lob:
            non_contextual_creation: true
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: mappings

jwt:
  secret: h3Kjud9HuTrFpPrHj3xxlP8joS/HDnKFy59bW8XAMs4=
  expiration: 3600000